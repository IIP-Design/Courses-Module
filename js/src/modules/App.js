var React = require('react');

require('./App.scss');

var App = React.createClass({
  render: function() {
    return (
      <div>
        <h1>Hello World!</h1>
      </div>
    );
  }
});

module.exports = App;
